<main class="contents-container">
  <div class="bredcrumb">
    <%= link_to "ホーム", root_path %> <span></span>新規登録
  </div>
  <div class="form-wrapper">
    <h3 class="form-title">アカウントを作成</h3>
    <%= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
      <%= render "devise/shared/error_messages", resource: resource %>

      <div class="row mb-3">
        <%= f.label :name, "ユーザー名", class: "col-sm-4 col-form-label required" %>
        <div class="col-sm-8">
          <%= f.text_field :name, class: "form-control", maxlength: 20, required: true %>
        </div>
      </div>
      
      <div class="row mb-3">
        <%= f.label :email, "メールアドレス", class: "col-sm-4 col-form-label required" %>
        <div class="col-sm-8">
          <%= f.email_field :email, autofocus: true, autocomplete: "email", class: "form-control", required: true %>
        </div>
      </div>

      <div class="row mb-3">
        <%= f.label :password, "パスワード（6文字以上）", class: "col-sm-4 col-form-label required" %>
        <div class="col-sm-8">
          <%= f.password_field :password, autocomplete: "new-password", class: "form-control", required: true %>
        </div>
      </div>

      <div class="row mb-3">
        <%= f.label :password_confirmation, "パスワード（確認用）", class: "col-sm-4 col-form-label required" %>
        <div class="col-sm-8">
          <%= f.password_field :password_confirmation, autocomplete: "new-password", class: "form-control", required: true %>
        </div>
      </div>

      <div class="agreement-box-wrapper">
        ※登録する場合、
        <%= link_to " 利用規約", terms_path %>
        と
        <%= link_to "プライバシーポリシー", policy_path %>
        に同意したものとみなします。
      </div>

      <div class="form-btn-wrapper">
        <div class="form-btn-first-line">
          <%= f.submit "登録する", class:"button-dark submit-btn button" %>
        </div>
        <div class="form-btn-second-line">
          <%= link_to user_twitter_omniauth_authorize_path do %>
            <button type="button" class="button-light twitter-btn">
              <%= image_tag "twitter-icon.png", size: "25x25" %>
              Twitterで登録
            </button>
          <% end %>
        </div>
      </div>
    <% end %>

    <div class="login-text">
      既にアカウントをお持ちの場合は<%= link_to "ログイン", new_session_path(resource_name) %>してください。</br>
      ゲストログインは<%= link_to 'こちら', users_guest_sign_in_path, method: :post %>から
    </div>
  </div>
</main>

<% if resource.errors.any? %>
  <%= javascript_pack_tag 'users/sign_up_render.js' %>
<%end%>
